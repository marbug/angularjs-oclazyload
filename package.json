{
  "name": "angularjs-oclazyload",
  "version": "1.0.0",
  "description": "* TODO",
  "scripts": {
    "start": "http-server ./ -a localhost -p 8000 -c-1",
    "start:win": "ws",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "angular": "^1.5.8",
    "angular-ui-router": "^1.0.22",
    "oclazyload": "^1.1.0"
  },
  "devDependencies": {
    "http-server": "^0.11.1",
    "local-web-server": "^2.6.1"
  }
}
